# my base config for a BT proxy using esp32c3
substitutions:
  name: bluetooth-test
  friendly_name: BT-Test
  version: "26.7.0.0"
  device_description: BT Test - made by ErKr - version ${version}.

packages:
  - !import common/esp32c3.yaml
  - !import common/btproxy.yaml
